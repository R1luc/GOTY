<header class="header">
  <div class="nav-left">
    <% if (user) { %>
      <span class="user-info">Bem-vindo, <strong><%= user.username %></strong></span>
      <a href="/users/logout" class="nav-link">Sair</a>
    <% } else { %>
      <a href="/users/login" class="nav-link">Entrar</a>
      <a href="/users/register" class="nav-link">Registrar</a>
    <% } %>
  </div>

  <div class="nav-right">
    <% if (user && user.id === 1) { %>
    <a href="/config/config" class="config-btn" title="Configurações">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <circle cx="12" cy="12" r="3"></circle>
        <path d="M12 1v6m0 6v6"></path>
        <path d="M4.22 4.22l4.24 4.24m3.08 3.08l4.24 4.24"></path>
        <path d="M1 12h6m6 0h6"></path>
      </svg>
    </a>
    <% } %>
  </div>
</header>
