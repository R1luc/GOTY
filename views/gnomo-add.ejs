<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Cadastro de Gnomos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/stylesheets/index.css" rel="stylesheet">
    <link href="/stylesheets/gnomos.css" rel="stylesheet">
</head>
<body>
    <%- include('partials/header') %>

    <main class="page-content container">

        <!-- Cadastro page hero/title -->
        <section class="gnomos-hero text-center mb-5">
            <h1 class="hero-title">CADASTRO</h1>
            <p class="text-muted small">Adicione um novo gnomo ao sistema</p>
        </section>

        <!-- Formulário -->
        <div class="row justify-content-center">
            <div class="col-12 col-md-8 col-lg-6">
                <form method="post" action="/gnomo/add" enctype="multipart/form-data" class="form-cadastro">
                    
                    <!-- Campo Nome -->
                    <div class="mb-4">
                        <label for="inputNome" class="form-label">Nome do Gnomo</label>
                        <input type="text" class="form-control form-input" name="nome_gnomo" id="inputNome" placeholder="Digite o nome" required>
                    </div>

                    <!-- Campo Foto -->
                    <div class="mb-4">
                        <label for="inputFoto" class="form-label">Foto do Gnomo</label>
                        <input type="file" class="form-control form-input" name="foto_gnomo" id="inputFoto" accept="image/*" required>
                    </div>

                    <!-- Campo Descrição -->
                    <div class="mb-4">
                        <label for="inputDesc" class="form-label">Descrição</label>
                        <textarea class="form-control form-input" name="desc_gnomo" id="inputDesc" rows="5" placeholder="Digite a descrição do gnomo" required></textarea>
                    </div>

                    <!-- Botões -->
                    <div class="d-flex gap-2 justify-content-end">
                        <button type="button" class="btn btn-secondary" onclick="history.back()">Voltar</button>
                        <button type="submit" class="btn btn-theme">+ Cadastrar</button>
                    </div>

                </form>
            </div>
        </div>

    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>